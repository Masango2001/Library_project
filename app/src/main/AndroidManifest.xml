<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bibliotheque"
        tools:targetApi="31">

        <!-- POINT D'ENTRÉE = DASHBOARD -->
        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- AUTRES ACTIVITÉS -->
        <activity android:name=".ui.auteurs.AuteurActivity" />
        <activity android:name=".ui.auteurs.AddAuteurActivity" />
        <activity android:name=".ui.categories.CategorieActivity" />
        <activity android:name=".ui.categories.AddCategorieActivity" />
        <activity android:name=".ui.livres.LivreActivity" />
        <activity android:name=".ui.livres.AddLivreActivity" />
        <activity android:name=".ui.membres.MembreActivity" />
        <activity android:name=".ui.membres.AddMembreActivity" />
        <activity android:name=".ui.emprunts.EmpruntActivity" />
        <activity android:name=".ui.emprunts.AddEmpruntActivity" />

    </application>

</manifest>